<%= form_with(model: [my_thread,rese], local: true) do |form| %>
   <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

      <%= form.submit %>
<% end %>
